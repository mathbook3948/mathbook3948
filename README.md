\* { margin: 0; padding: 0; } .main-font { font-family: "Poppins","Jua", serif; font-optical-sizing: auto; } .container { display: grid; width: inherit; font-family: "Lora", serif; font-optical-sizing: auto; } #main-container { display: grid; width: fit-content; padding: 20px; border-radius: 30px; color: white; background-color: rgba(0, 0, 0, 0.7); padding-bottom: 25px; } .lang-chart { display: flex; justify-content: center; /\* 중앙 정렬 \*/ width: 15em; align-items: center; } #welcome { font-size: 1.5em; white-space: nowrap; font-weight: bold; } #goodLang-container { display: grid; grid-template-rows: auto 1fr; } #goodLang-title { font-size: 1em; height: 20px; } #chart-container { display: flex; justify-content: center; direction: ltr; margin: 0; } #header { margin-bottom: 5px; } #goodLang-content { display: flex; }      

자신있는 언어

안녕하세요! 07년생 개발자입니다

// Thymeleaf에서 전달된 language\_percent를 JavaScript 객체로 삽입 const profile = {"language\_percent":{"language\_percent":{"TypeScript":0.06,"Java":0.88,"CSS":0.01,"HTML":0.05}},"color":{"Java":"rgba(255, 140, 0)"},"userName":"mathbook3948"}; // 서버 측에서 profile 데이터를 전달해야 합니다. console.log(profile); // 데이터를 Chart.js 형식에 맞게 변환 const labels = Object.keys(profile.language\_percent.language\_percent); const dataValues = Object.values(profile.language\_percent.language\_percent); // 비율을 100배로 변환 const dataValues100x = dataValues.map(value => value \* 100); console.log(labels, dataValues); // 언어별 색상 매핑 const languageColors = { "Java": "rgba(255, 140, 0)", // Bright Orange "TypeScript": "rgba(45, 150, 255)", // Azure Blue "Python": "rgba(50, 175, 75)", // Forest Green "JavaScript": "rgba(255, 85, 95)", // Coral Red "CSharp": "rgba(163, 73, 164)", // Royal Purple "Go": "rgba(0, 172, 215)", // Cyan "Ruby": "rgba(200, 20, 60)", // Deep Red "PHP": "rgba(79, 93, 149)", // Slate Blue "C++": "rgba(0, 135, 107)", // Teal "Rust": "rgba(183, 65, 14)", // Rust Brown "Kotlin": "rgba(241, 142, 51)", // Light Orange "Swift": "rgba(252, 82, 48)", // Vermillion "Scala": "rgba(222, 52, 66)", // Crimson "HTML": "rgba(228, 77, 38)", // HTML Orange "CSS": "rgba(33, 150, 243)", // Material Blue "SQL": "rgba(25, 118, 210)", // Dark Blue "R": "rgba(68, 184, 172)", // Seafoam Green "Perl": "rgba(255, 128, 171)", // Pink "Lua": "rgba(0, 176, 185)", // Turquoise "MATLAB": "rgba(0, 118, 168)", // Marine Blue "GoLang": "rgba(41, 41, 41)", // Dark Gray "Haskell": "rgba(145, 65, 172)", // Amethyst "Fortran": "rgba(239, 127, 26)", // Burnt Orange "VB.NET": "rgba(2, 136, 99)", // Emerald Green "Elixir": "rgba(153, 51, 255)", // Purple "Clojure": "rgba(91, 206, 250)", // Light Blue "F#": "rgba(178, 157, 243)", // Lavender "Shell": "rgba(236, 212, 99)", // Sand Yellow "Objective-C": "rgba(67, 160, 71)", // Material Green "Dart": "rgba(26, 35, 126)", // Indigo "Assembly": "rgba(121, 85, 72)", // Brown "VB": "rgba(255, 193, 7)", // Amber "ActionScript": "rgba(156, 39, 176)", // Deep Purple "Ada": "rgba(69, 90, 100)", // Blue Gray "ABAP": "rgba(213, 0, 249)" // Magenta }; // 언어에 맞는 색상 배열 생성 const dynamicBackgroundColor = labels.map(language => languageColors\[language\] || 'rgba(200, 200, 200)'); // 기본 색상 (회색) const dynamicBorderColor = dynamicBackgroundColor.map(color => color.replace('rgba', 'rgba').replace(')', ', 1)')); // 차트 데이터 설정 const data = { labels: labels, datasets: \[{ label: '언어 사용 비율(%)', data: dataValues100x, backgroundColor: dynamicBackgroundColor, borderColor: dynamicBorderColor, borderWidth: 1 }\] }; // 차트 옵션 설정 const config = { type: 'doughnut', data: data, options: { responsive: true, maintainAspectRatio: true, // 비율 유지하지 않도록 설정 plugins: { legend: { position: 'top', labels: { color: 'white' } }, title: { display: false, } } } }; // 차트 생성 const myChart = new Chart(document.getElementById('myChart'), config); //--------------------------------------------------------------------------------------- console.log(profile); const goodLangColors\_keys = Object.keys(profile.color); const goodLangColors\_values = Object.values(profile.color); console.log("--------------------------------") console.log(goodLangColors\_keys, goodLangColors\_values); const goodLang\_content = document.getElementById("goodLang-content"); goodLang\_content.style.marginTop = "0.5em"; goodLangColors\_keys.forEach((key, index) => { const div = document.createElement("div"); div.style.backgroundColor = goodLangColors\_values\[index\]; div.style.width = "fit-content"; div.style.height = "fit-content"; div.style.marginRight = "0.3em" div.style.padding = "0.5em" div.style.paddingTop = "0.25em" div.style.paddingBottom = "0.25em" div.style.borderRadius = "5px"; div.style.display = "flex"; div.innerHTML = key; goodLang\_content.appendChild(div); }) document.getElementById("welcome").innerHTML = \`Welcome to My Github!\`;
